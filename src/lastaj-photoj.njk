---
layout: layout.njk
title: Lastaj photoj
permalink: /lastaj-photoj/
---

<header class="page-header">
    <h1>Lastaj photoj</h1>
    <p class="subtitle">Kvindek plej novaj fotoj en nia retejo</p>
</header>

<main class="main-content">
    <div class="gallery-controls">
        <p>Jen la 50 plej lastaj fotoj (ĉiuj lensoj).</p>
    </div>

    <div class="gallery-grid" id="gallery-grid">
        {# collections.allImages is already sorted newest-first in .eleventy.js #}
        {# Loop and limit to 50 using loop.index0 to avoid relying on the Nunjucks 'slice' filter semantics #}
        {% for image in collections.allImages %}
            {% if loop.index0 < 50 %}
        <div class="gallery-item" data-lens="{{ image.lens }}">
            <img
                src="{{ image.thumb.src }}"
                srcset="{{ image.thumb.srcset }}"
                sizes="{{ image.thumb.sizes }}"
                alt="{{ image.lens }} - {{ image.filename }}"
                data-full="{{ image.path }}"
                loading="{{ 'eager' if loop.index0 < 6 else 'lazy' }}"
                decoding="async"
                fetchpriority="{{ 'high' if loop.index0 < 6 else 'low' }}"
                class="gallery-img"
                {% if image.thumb.width %} width="{{ image.thumb.width }}"{% endif %}
                {% if image.thumb.height %} height="{{ image.thumb.height }}"{% endif %}
            >
            <div class="image-info">
                <span class="lens-name">{{ image.lens }}</span>
                <span class="file-title">{{ image.filename }}</span>
            </div>
        </div>
            {% endif %}
        {% endfor %}
    </div>
</main>
